import 'package:flutter/material.dart';

class News_Page extends StatelessWidget {
  const News_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Hello from news"),
    );
  }
}
